<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/lg_quizz"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".UI.Activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các màn hình Đăng nhập/Đăng ký/Quên mật khẩu -->
        <activity android:name=".UI.Activity.SignUpActivity" />
        <activity android:name=".UI.Activity.LoginActivity" />
        <activity android:name=".UI.Activity.ForgotPasswordActivity" />
        <activity android:name=".UI.Activity.UpdatePasswordActivity" />

        <!-- Các màn hình Quiz -->
        <activity android:name=".UI.Activity.QuizListActivity" />
        <activity android:name=".UI.Activity.QuizQuestions1Activity" />
        <activity android:name=".UI.Activity.QuizQuestions2Activity" />
        <activity android:name=".UI.Activity.QuizQuestions3Activity" />
        <activity android:name=".UI.Activity.QuizQuestions4Activity" />
        <activity android:name=".UI.Activity.AnswerSavedActivity" />
        <activity android:name=".UI.Activity.SaveActivity" />
        <activity android:name=".UI.Activity.SeeScoreActivity" />
        <activity android:name=".UI.Activity.SeeDetailsActivity" />

        <!-- Các màn hình Chat -->
        <activity android:name=".UI.Activity.MainChatActivity" />
        <activity android:name=".UI.Activity.ChatActivity" />

        <!-- Các màn hình Khác -->
        <activity android:name=".UI.Activity.ProfileActivity" />
        <activity android:name=".UI.Activity.SettingsActivity" />


    </application>

</manifest>